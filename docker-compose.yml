version: "3.9"

services:

  web:
    build: .
    ports:
      - "5000:5000"

    depends_on:
      - mysql

  mysql:
    image: mysql:8

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: food_order_db

    ports:
      - "3306:3306"